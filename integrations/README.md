# Integrations placeholder

Fill tokens in `.env` (never commit real secrets):
- `PUSH_FIREBASE_TOKEN`, `PUSH_ONESIGNAL_TOKEN`
- `OAUTH_GOOGLE_KEY`, `OAUTH_APPLE_KEY`
- `ANALYTICS_TOKEN`

Backend reads these values from environment via `backend/config.py`.
